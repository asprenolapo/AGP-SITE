//==========================
// NOTIFICATION MODULE
//==========================

export function showNotification(text = "Default notification text") {
  // You can fill this function to show a custom notification in the centre of the page for example
  alert(text);
}
